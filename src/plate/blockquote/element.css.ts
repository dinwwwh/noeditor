import { rem, style } from '@fizzui/styles'

export const blockquote = style({
  marginTop: rem(4),
  marginBottom: rem(4),
  borderLeftWidth: rem(2),
  paddingLeft: rem(24),
  fontStyle: 'italic',
})
